{
  "name": "com.cot.hypernet",
  "version": "1.0.4",
  "description": "Share internet access for android mobile phones with a line.",
  "main": "index.js",
  "devDependencies": {
    "electron": "^10.0.0"
  },
  "scripts": {
    "start": "electron .",
    "pack": "electron-packager . Cot-Hypernet --all --out ./built --app-version 1.2.1 --overwrite --icon bin/icon.png --asar   &&   xcopy bin built\\Cot-Hypernet-win32-x64\\bin\\"
  },
  "author": "Cot",
  "dependencies": {
    "node-cmd": "^3.0.0"
  }
}
